import * as React from 'react';
import { useRef } from 'react';
import { Animated,View } from 'react-native';

//TODO header extended with round borders should by scrolling retract to normal header, maybe with picture as background for profiles..
export default function AnimatedHeader(){

    return(
        <></>
    )
}